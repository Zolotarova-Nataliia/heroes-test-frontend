export const getHeroes = (state) => state.heroes.heroesList;
export const getHeroesCount = (state) => state.heroes.count;
export const getCurrentHero = (state) => state.heroes.currentHero;
export const getModalIsOpen = (state) => state.heroes.modalIsOpen;
